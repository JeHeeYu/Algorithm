#include <stdio.h>

int main() 
{
	int n, i, j, input;
	scanf("%d", &n);

	for (i = 0; i < n; i++)
	{
		scanf("%d", &input);
		for (j = 0; j < input; j++)
			printf("=");
		printf("\n");
	}

	return 0;
}
