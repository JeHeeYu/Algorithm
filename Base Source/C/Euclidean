#include <stdio.h>

int GCD(int x, int y)
{
	if (y == 0)
		return x;
	else
	{
		return Euclidean(y, x % y);
	}

}

int LCM(int x, int y)
{
	return a * b / GCD(a, b);
}

int main()
{
	int x, y;
	scanf("%d %d", &x, &y);
	printf("%d\n", GCD(x, y));
	printf("%d\n", LCM(x, y));
}
