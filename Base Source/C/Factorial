#include <stdio.h>

int Factorial(int n)
{
	if (n > 0)
	{
		return n * Factorial(n - 1);
	}
	else
	{
		return 1;
	}
}

int main()
{
	int n;
	printf("정수 입력 : ");
	scanf("%d", &n);
	printf("%d! = %d\n", n, Factorial(n));

	return 0;
}
